# MarkItDown — Resume Extraction Evaluation
# Python 3.10+ required

# Core: MarkItDown with PDF + DOCX + image support
markitdown[pdf,docx,image]>=0.1.4

# For scanned PDF / image detection (no extra OCR — we log the gap honestly)
pymupdf>=1.24.0

# For metrics tracking and reporting
tabulate>=0.9.0